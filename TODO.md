# TODO

Ideas to consider, not yet planned into a phase. Each item is a one-line
title with the problem, a sketch of the options, and a short "decide
later" note. Keep entries terse — this file is a thinking pad, not a
specification.

---

## ~~Easier global invocation~~ — done

**Resolved by:** `pipx install git+https://github.com/.../claude-config-auditor.git`
is now the recommended install path (see README "Install"). `claude-audit`
ends up on `PATH` and is unaffected by any other project's active venv.
Publishing to PyPI is still a future option but no longer urgent.

---
